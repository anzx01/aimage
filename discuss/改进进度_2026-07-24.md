# AIMAGE 改进进度（2026-07-24）

> 完整计划见：`C:\Users\admin\.claude\plans\g-aiprj-aimage-newg1-md-pure-hearth.md`
> 本文件用于记录实施进度，方便下次续接。

## 已确认的关键决策
- **架构方向 A**：后端 FastAPI 为唯一可信服务端，前端只做 UI + Auth + 只读；所有敏感写（建项目/扣费/购买/退款/生成）走后端。
- **数据表真源**：`profiles`（用户）/ `credit_transactions.transaction_type` / `projects.project_type`。已删 `database/` 旧 `users` schema。
- **鉴权基线**：Supabase 原生 session token（`supabase_admin.auth.get_user`）。废弃后端自建 JWT / `security.py` / `python-jose` / `passlib`。
- **积分机制**：移除隐晦的"插入任务即自动扣费"触发器，改为显式 RPC（`deduct_user_credits` / `refund_user_credits`，仅 `service_role` 可执行），后端服务层统一调用。
- **范围**：P0 + P1 + P2 全量。Seedance 密钥由用户后续提供。

## ✅ 已完成：P0-1 数据层统一
改动文件：
- 新增 `supabase/migrations/20260724000000_converge_schema_and_credits.sql`
  - `profiles` 加 `is_admin`；幂等兜底 `avatar_url`
  - **收紧 profiles 更新权限（列级 GRANT）**：`REVOKE UPDATE ... FROM authenticated` + 仅 `GRANT UPDATE(full_name, avatar_url, phone)`。堵住"浏览器端直接改 credits 刷积分"的根因。
  - `DROP` 自动扣费触发器 `deduct_credits_on_generation_task` 及函数 `deduct_credits_on_task_creation`
  - 新建 `deduct_user_credits` / `refund_user_credits` RPC（SECURITY DEFINER + 行锁 + 余额检查 + 写流水），执行权限仅授予 `service_role`
- 新增 `supabase/migrations/20260724000100_create_assets_bucket.sql`（把原 `database/storage_policies.sql` 的 assets bucket 策略收敛为正式迁移）
- 修正 `supabase/migrations/20260216000000_add_avatar_support.sql`：`ALTER TABLE users` → `ALTER TABLE profiles`（原来针对不存在的 users 表，会导致全新库迁移中断）
- 删除：`database/`（整个目录：init_database_v3.sql、migrations/003、storage_policies.sql）、`supabase/complete_migration.sql`

⚠️ 待办（需在部署环境执行迁移）：把两个新迁移应用到 Supabase；验证 `profiles`/`credit_transactions`/`projects` 字段与代码一致、列级权限生效（浏览器端改 credits 应被拒）。

## ⬜ 未开始：P0-2 统一鉴权（下次从这里继续）
计划步骤：
1. 新建 `backend/app/api/deps.py`：单一 `get_current_user_id`（`supabase_admin.auth.get_user(token)`）；另加 `get_current_admin_id`（查 `profiles.is_admin`）供 P0-4 用。
2. `projects.py` / `digital_humans.py` / `generate.py` / `credits.py` 全部改用 `deps`，删除各自重复的 `get_current_user_id`、函数内 `import jwt`、无用 import（`digital_humans.py` 顶部 `import jwt` 实际未用）。
3. `auth.py` 的 `/register`、`/login` 改为返回 Supabase session token（`auth_response.session.access_token`）而非自建 JWT；`/logout` 修正说明。
4. 删除 `security.py` 的 JWT/bcrypt 死代码；`requirements.txt` 移除 `python-jose`、`passlib`（确认无引用后）。

## ⬜ 未开始：P0-3 ~ P2（详见计划文件）
- **P0-3 积分收敛**：新建 `backend/app/services/credits_service.py` 封装两个 RPC；`generate.py` 建任务前显式扣费、失败退款；`digital_humans.py` 改调用 service（替换现有手动扣费）；修复并注册 `credits.py`（导入改 `app.db.supabase`、依赖用 deps、表名 `profiles`、字段 `transaction_type`、`main.py` 注册路由）；`purchase` 标注沙箱、按 `ENVIRONMENT` 开关；**前端删除** `frontend/lib/credits.ts` 写操作 + `frontend/app/api/credits/{deduct,refund,purchase}/route.ts`，`generate/page.tsx` 改为一次后端调用。
- **P0-4 安全**：admin 后端路由 + 前端拦截（`is_admin`）；文件上传后端二次校验；删 `generate.py` 8 处 `print("[DEBUG]")` 与前端打印 token 的 `console.log`；`config.py` `DEBUG` 默认 `False`。
- **P0-5 清理**：`seedance` 分支给明确"未配置"响应；`backend/add_credits.py`、`test_digital_human.py` 移入 `backend/scripts/` 并加 `__main__` 保护/改名；删 `sqlalchemy`/`alembic` 冗余依赖。
- **P1**：接入 Seedance（待密钥）；数字人诚实化（默认降级为"AI 口播/文生视频"文案）；purchase 演示标注；移除 `admin`/`credits`/`showcase` 假数据静默回退。
- **P2**：`scripts/` 启停脚本 + `logs/` 文件日志；`main.py` 全局异常处理 + `slowapi` 限流 + `/health` 查库；清理 NeoBund1 残留（`config.py` `APP_NAME`、`.lane/plans`）；根目录过时报告移入 `docs/archive/`；更新 `README.md`（Next.js 16、真实功能状态、正确启动方式、迁移文件引用）。

## 续接要点备忘
- 前端实际用的是 **Supabase session token**（`store.ts` 用 `signInWithPassword`，`generate/page.tsx` 用 `session.access_token`），后端自建 JWT 前端从未使用——这是鉴权统一的依据。
- `digital_humans.py` 的"校验→扣费→失败退款→写流水"是全项目唯一正确的积分范例，可作为收敛模板（但要改为调用新 RPC）。
- `credit_transactions` 表真实字段为 `transaction_type` + `balance_after` + `reference_id`/`reference_type`（**不是** `type`/`related_project_id`——旧代码用错列名导致写入失败）。
- `projects` 表**无 `mode` 列**，只有 `project_type`；`digital_humans.py` 往 projects 插入时写了 `"mode"`，需在 P0-3 一并改掉。
